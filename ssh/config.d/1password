Host *
  IdentityAgent ~/.1password/agent.sock

# macOS-specific override (auto-detected by 1Password app)
Match exec "uname -s | grep -q Darwin"
  IdentityAgent "~/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"
